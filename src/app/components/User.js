import React from "react";
import { browserHistory } from "react-router";

export class User extends React.Component {
    render() {
        return (
            <div>
                <h2>Backpage example</h2>
            </div>
        );
    }
}